# Copy to config.local and set your PhysioNet username.
# config.local is gitignored; never commit real credentials.
# Get an account at https://physionet.org/register/

export PHYSIONET_USER="vasudev13"
